import { describe, it, expect } from 'vitest'
import ManualEntry from './ManualEntry'

describe('ManualEntry', () => {
  it('exports a component function', () => {
    expect(typeof ManualEntry).toBe('function')
  })
})
