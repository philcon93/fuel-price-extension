import { describe, it, expect } from 'vitest'
import { TrimPicker } from './TrimPicker'

describe('TrimPicker', () => {
  it('exports a component function', () => {
    expect(typeof TrimPicker).toBe('function')
  })
})
