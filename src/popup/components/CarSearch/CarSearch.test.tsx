import { describe, it, expect } from 'vitest'
import CarSearch from './CarSearch'

describe('CarSearch', () => {
  it('exports a component function', () => {
    expect(typeof CarSearch).toBe('function')
  })
})
